{% extends 'base.html.twig' %}

{% block title %}Administration - régimes{% endblock %}

{% block body %}

    <div class="mb-4 mt-4">
        <a type="button" class="btn btn-custom" href="{{ path('admin_home') }}"><img src="{{ asset('assets/images/arrow-left-circle.svg') }}" alt=""> Panneau d'administration</a>
    </div>

    <h1  class="text-center">Liste des régimes</h1>
    <div class="mb-4 mt-4">
        <a href="{{ path('admin_diet_add') }}" type="button" class="btn btn-custom">
            Ajouter un régime
        </a>
    </div>
            

<table class="table table-bordered table-secondary border-dark align-middle table-hover">
    <thead>
        <tr class="text-center">
            <th>Nom</th>
            <th colspan="2">Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for diet in diets %}
        <tr class="text-center align-middle">
            <td>{{ diet.name }}</td>
            <td>
            <a href="{{ path('admin_diet_edit', {id: diet.id}) }}" type="button" class="btn btn-success-custom">
                Modifier
            </a>
            </td>
            <td>
            <a href="{{ path('admin_diet_delete', {id: diet.id}) }}" type="button" class="btn btn-danger-custom">
                Supprimer
            </a>
            </td>
        </tr>
    {% endfor %}
        
    </tbody>
</table>

    {# display navigation #}
    <div class="container text-center">
        <div class="row justify-content-center">
            <div class="navigation col-4">
                {{ knp_pagination_render(diets) }}
            </div>
        </div>
    </div>

{% endblock %}
